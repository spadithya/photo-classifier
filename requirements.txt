# ============================================================================
# Photo Classifier — Python dependencies
# ============================================================================
# IMPORTANT (GPU): do NOT `pip install -r requirements.txt` for torch first.
# The default PyPI torch wheel is CPU-only. To get the CUDA build for your
# RTX 3060, install torch + torchvision FIRST from the CUDA wheel index:
#
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#
# Then `pip install -r requirements.txt` to get everything else. Pip will see
# torch is already satisfied and won't overwrite it with the CPU build.
# (Python 3.13 needs torch >= 2.6 — older wheels have no cp313 build.)
# ============================================================================

# Core scientific stack
numpy>=1.26
matplotlib>=3.8
seaborn>=0.13
pandas>=2.1

# Transfer learning + image handling
torch>=2.6
torchvision>=0.21
pillow>=10.2
pillow-heif>=0.16      # decode iPhone .heic photos

# Evaluation
scikit-learn>=1.4      # confusion_matrix, classification_report

# CLI niceties
tqdm>=4.66             # progress bars

# Streamlit app
streamlit>=1.32
